// Generated by jextract

package io.surfworks.warpforge.io.ffi.ucx;

import java.lang.invoke.*;
import java.lang.foreign.*;
import java.nio.ByteOrder;
import java.util.*;
import java.util.function.*;
import java.util.stream.*;

import static java.lang.foreign.ValueLayout.*;
import static java.lang.foreign.MemoryLayout.PathElement.*;

/**
 * {@snippet lang=c :
 * typedef ucs_status_t (*ucp_am_recv_callback_t)(void *, const void *, size_t, void *, size_t, const ucp_am_recv_param_t *)
 * }
 */
public class ucp_am_recv_callback_t {

    ucp_am_recv_callback_t() {
        // Should not be called directly
    }

    /**
     * The function pointer signature, expressed as a functional interface
     */
    public interface Function {
        int apply(MemorySegment arg, MemorySegment header, long header_length, MemorySegment data, long length, MemorySegment param);
    }

    private static final FunctionDescriptor $DESC = FunctionDescriptor.of(
        Ucx.C_INT,
        Ucx.C_POINTER,
        Ucx.C_POINTER,
        Ucx.C_LONG,
        Ucx.C_POINTER,
        Ucx.C_LONG,
        Ucx.C_POINTER
    );

    /**
     * The descriptor of this function pointer
     */
    public static FunctionDescriptor descriptor() {
        return $DESC;
    }

    private static final MethodHandle UP$MH = Ucx.upcallHandle(ucp_am_recv_callback_t.Function.class, "apply", $DESC);

    /**
     * Allocates a new upcall stub, whose implementation is defined by {@code fi}.
     * The lifetime of the returned segment is managed by {@code arena}
     */
    public static MemorySegment allocate(ucp_am_recv_callback_t.Function fi, Arena arena) {
        return Linker.nativeLinker().upcallStub(UP$MH.bindTo(fi), $DESC, arena);
    }

    private static final MethodHandle DOWN$MH = Linker.nativeLinker().downcallHandle($DESC);

    /**
     * Invoke the upcall stub {@code funcPtr}, with given parameters
     */
    public static int invoke(MemorySegment funcPtr,MemorySegment arg, MemorySegment header, long header_length, MemorySegment data, long length, MemorySegment param) {
        try {
            return (int) DOWN$MH.invokeExact(funcPtr, arg, header, header_length, data, length, param);
        } catch (Throwable ex$) {
            throw new AssertionError("should not reach here", ex$);
        }
    }
}

