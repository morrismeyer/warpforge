name: Lab CI (NUC + NVIDIA)

on:
  push:
    branches:
      - main

jobs:
  orchestrated-lab-ci:
    runs-on: self-hosted

    steps:
      - name: Run orchestrated build on nuc (then nvidia)
        run: |
          echo "Starting orchestrated lab CI from GitHub Actions on nuc..."
          /home/actions/surfworks/warpforge/holmes-lab/mark1/ci-scripts/orchestrate-nuc-build.sh
