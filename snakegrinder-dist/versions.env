# ============================================================================
# SnakeGrinder Distribution - Version Configuration
# ============================================================================
#
# Update these versions when upgrading dependencies.
# The build scripts source this file automatically.
#
# UPGRADE CHECKLIST:
# 1. Check GraalPy releases: https://github.com/oracle/graalpython/releases
# 2. Get PyTorch patch from a full GraalPy distribution (Oracle GraalPy, not GitHub release)
#    - Patches are in: $GRAALPY_HOME/lib/graalpy*/patches/torch-*.patch
#    - Copy to: scripts/patches/torch-<version>.patch
# 3. Update versions below
# 4. Rebuild: ./gradlew :snakegrinder-dist:rebuildPytorchVenv
# 5. Test: ./gradlew :snakegrinder-dist:assembleDist
#
# NOTE: GraalPy GitHub releases don't include PyTorch patches. We bundle them
# in scripts/patches/ so builds work on any platform.
#
# ============================================================================

# GraalPy version (check https://github.com/oracle/graalpython/releases)
GRAALPY_VERSION="25.0.1"

# PyTorch version (must have matching GraalPy patch)
# Check $GRAALPY_HOME/lib/graalpy*/patches/ for available patches
PYTORCH_VERSION="2.7.0"

# Python version used by GraalPy (changes with GraalPy major versions)
PYTHON_VERSION="3.12"
