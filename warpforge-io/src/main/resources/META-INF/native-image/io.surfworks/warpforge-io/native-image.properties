# Native-image configuration for warpforge-io UCC performance test
# Enables FFM (Foreign Function & Memory) API support

Args = \
    --enable-preview \
    -H:+ForeignAPISupport \
    -J--enable-native-access=ALL-UNNAMED \
    --add-modules=jdk.incubator.vector
